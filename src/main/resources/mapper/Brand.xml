<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="top.sdaily.mapper.BrandMapper">
    <select id="test" resultType="top.sdaily.model.Brand">
        SELECT * FROM bm_brand
    </select>
</mapper>